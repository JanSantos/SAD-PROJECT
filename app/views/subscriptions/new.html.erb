

<%= simple_nested_form_for([@subscriber, @subscription]) do |f| %>
  <%= f.error_notification %>
<h1>Add Subscription to <i><%= @subscriber.name %></i></h1>
  
  <h2>Subscription Type: <i><%= @subscriber.subscription_type %></i></h2>

  <h2><%= f.association :journal %></h2>

  <% m.errors.each do |attr,msg| %>
  <% msg %>
  <% end %>
  

  

  <%= f.button :submit %>

  <% end %>
  <%= button_to "Cancel", subscribers_path, method: :get, data: { confirm: "Are you sure?" } %>


  
