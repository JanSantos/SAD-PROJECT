

<%= simple_nested_form_for([@journal, @issue]) do |f| %>
  <%= f.error_notification %>
Add Issue to <i><%= @journal.title %></i>
  </br>
  </br>

  <b><%= f.input :volume_number %></b>
  <b><%= f.input :issue_number %></b>
  <b><%= f.input :date_published %></b>
  
</br>
  

  <%= f.button :submit %>

  <% end %>
  <%= button_to "Cancel", journals_path, method: :get, data: { confirm: "Are you sure?" } %>


  
